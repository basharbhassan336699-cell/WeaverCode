#!/usr/bin/env bash
# WeaverCode launcher 🕸️ (Termux / Linux / macOS)
# Usage: ./weaver <command>   e.g.  ./weaver start
ROOT="$(cd "$(dirname "$0")" && pwd)"
PY="$(command -v python3 || command -v python)"
exec "$PY" "$ROOT/weaver-cli.py" "$@"
